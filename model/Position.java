package csulb.cecs323.model;

/**
 * Enumeration for the positions a player can have
 * Can be top, mid, jungle, support, adc
 */
public enum Position {
    top,
    mid,
    jungle,
    support,
    adc
}
